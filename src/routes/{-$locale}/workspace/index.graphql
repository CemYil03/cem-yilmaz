# Workspace hub query — fetches lightweight counts for the focus-area
# tile badges (projects inbox, open standalone todos). Hub is rendered on
# every workspace surface visit, so this stays cheap and `cache-first`.

query WorkspaceHub {
    sessionFindOne {
        user {
            admin {
                adminProjectRequestInboxCount
                adminStandaloneTaskOpenCount
            }
        }
    }
}
